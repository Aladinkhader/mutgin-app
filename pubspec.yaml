name: mutqin
description: تطبيق متقن لتسميع القرآن الكريم ومتابعة الحفظ.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  permission_handler: ^13.0.2
  path_provider: ^2.1.5
  whisper_ggml: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
