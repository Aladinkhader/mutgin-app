import 'package:flutter/material.dart';

abstract final class AppColors {
  // Main brand colors
  static const Color deepNight = Color(0xFF0B1110);
  static const Color forest = Color(0xFF123C35);
  static const Color emerald = Color(0xFF1D6B5C);

  // Premium accent
  static const Color gold = Color(0xFFD6B56A);
  static const Color softGold = Color(0xFFE8D39B);

  // Surfaces
  static const Color surface = Color(0xFF111918);
  static const Color surfaceElevated = Color(0xFF172220);
  static const Color surfaceSoft = Color(0xFF1D2927);

  // Text
  static const Color textPrimary = Color(0xFFF7F4EC);
  static const Color textSecondary = Color(0xFFB8C2BE);
  static const Color textMuted = Color(0xFF7F8C87);

  // Semantic colors
  static const Color success = Color(0xFF55C99A);
  static const Color error = Color(0xFFE97979);
  static const Color warning = Color(0xFFE6B85C);

  // Utility
  static const Color transparent = Colors.transparent;
}
